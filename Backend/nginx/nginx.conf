events {}
http {
    server {
        listen 80; # there it will atomatically insert the TCP man , TCP socket on port 80
        location / {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}